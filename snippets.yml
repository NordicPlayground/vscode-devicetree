---

New GPIO:
  prefix: "gpio"
  body: |
    gpio${1}: gpio@${2} {
        compatible = "${3:nordic,nrf-gpio}";
        status = "${4|okay,disabled|}";
        label = "${5:GPIO_$1}";
    };
  description: "Adds new GPIO port section"
